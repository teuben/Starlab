#include "tdyn.h"
#include "hdyn.h"
#include "worldline.h"

main()
{
    // Interesting information:

    PRL(sizeof(node));
    PRL(sizeof(dyn));
    PRL(sizeof(tdyn));
    PRL(sizeof(_dyn_));
    PRL(sizeof(hdyn));
    PRL(sizeof(segment));
    PRL(sizeof(worldline));
    PRL(sizeof(hydrobase));
    PRL(sizeof(starbase));
}
