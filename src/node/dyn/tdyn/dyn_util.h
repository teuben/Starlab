
// From dyn_util.C:

void convert_relative_to_absolute(dyn*);
void accumulate_potential_energy(dyn*, dyn*, real&, real&, dynptr&);
void compute_energies(dyn*, dyn*, char&);
int nearest_index(dyn* b, float, float, int, int);
int count_nodes(dyn*);

